/*const createProducer= require('../createProducer');

const createEventBookRequest=async(message)=>{
    await createProducer('Booking-requist',{
        key:message.orderId,
        value:JSON.stringify(message),
    });
    }

    module.exports={createEventBookRequest};*/